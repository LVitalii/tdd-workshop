function test() {
    return true;
}

function calcAdd(first, second) {
    return parseInt(first) + parseInt(second);
}

function calcSubstr(first, second) {
    return parseInt(first) - parseInt(second);
}

function calcMulti(first, second) {
    return parseInt(first) * parseInt(second);
}

function calcDiv(first, second) {
    return parseInt(first) / parseInt(second);
}